import styled from "styled-components";

const gray = "#808080"

export const SearchBar = styled.article`
background-color: ${gray};
padding: 3rem;
input { 
    display: block;
    margin : auto;
    width: 300px
}
`
